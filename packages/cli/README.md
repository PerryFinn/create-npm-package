# @perryfinn/cli

PerryFinn monorepo 的命令行工具包。

## 安装

```bash
bun add -g @perryfinn/cli
```

## 使用

```bash
perryfinn
perryfinn --help
```

该包以命令行工具形式发布，不提供公共 import API。请通过 `perryfinn` bin 入口使用。

## 发布内容

发布包包含编译后的 CLI bundle、source map、本文档和 `LICENSE.txt`。
