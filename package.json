{
  "name": "perryfinn-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "Bun workspace monorepo for PerryFinn packages",
  "keywords": [],
  "license": "MIT",
  "author": "PerryFinn <perryfinn@163.com>",
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "@types/bun": "^1.3.13",
      "@types/node": "22.18.1",
      "typescript": "^6.0.3"
    },
    "catalogs": {
      "build": {
        "@arethetypeswrong/cli": "^0.18.2",
        "tsdown": "^0.21.10"
      },
      "testing": {
        "@vitest/coverage-v8": "^4.1.5",
        "vitest": "^4.1.5"
      },
      "quality": {
        "@biomejs/biome": "^2.4.13"
      }
    }
  },
  "scripts": {
    "build": "bun run --workspaces build",
    "check:exports": "bun run --filter @perryfinn/utils check:exports",
    "check:type": "bun run --workspaces check:type",
    "ci": "bun run lint && bun run check:type && bun run test && bun run build && bun run check:exports",
    "lint": "bun run --workspaces lint",
    "lint:fix": "bun run --workspaces lint:fix",
    "prepare": "bunx husky",
    "release:publish": "changeset publish",
    "release:version": "changeset version",
    "test": "bun run --workspaces test",
    "test:coverage": "bun run --workspaces test:coverage"
  },
  "lint-staged": {
    "**/package.json": [
      "sort-package-json",
      "biome check --write --no-errors-on-unmatched"
    ],
    "*.{js,jsx,ts,tsx}": [
      "biome check --write --no-errors-on-unmatched"
    ],
    "*.{css,json,jsonc,graphql,gql}": [
      "biome check --write --no-errors-on-unmatched"
    ],
    "*": [
      "secretlint"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:quality",
    "@changesets/cli": "^2.31.0",
    "@changesets/types": "^6.1.0",
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0",
    "@commitlint/types": "^20.5.0",
    "@secretlint/secretlint-rule-preset-recommend": "^11.7.1",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "secretlint": "^11.7.1",
    "sort-package-json": "^3.6.1",
    "typescript": "catalog:"
  },
  "engines": {
    "bun": ">=1.0.0",
    "node": ">=22"
  },
  "volta": {
    "node": "24.14.0"
  }
}
