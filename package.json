{
  "name": "create-npm-package",
  "version": "1.0.5",
  "author": "PerryFinn <perryfinn@163.com>",
  "homepage": "https://github.com/PerryFinn/create-npm-package#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PerryFinn/create-npm-package.git"
  },
  "bugs": {
    "url": "https://github.com/PerryFinn/create-npm-package/issues"
  },
  "keywords": [
    "npm",
    "typescript",
    "package"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "biome check . --no-errors-on-unmatched",
    "lint:fix": "biome check --write . --no-errors-on-unmatched",
    "build": "tsup",
    "check:exports": "attw --pack . --ignore-rules=cjs-resolves-to-esm --profile=node16",
    "release:local": "changeset version && changeset publish",
    "prepublishOnly": "bun run ci",
    "ci": "bun run lint && bun run typecheck && bun test && bun run build && bun run check:exports",
    "prepare": "husky",
    "precommit": "bun run lint && bun run typecheck && bun run test"
  },
  "main": "dist/index.js",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@biomejs/biome": "2.2.2",
    "@changesets/cli": "^2.29.6",
    "@changesets/types": "^6.1.0",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@commitlint/types": "^19.8.1",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "description": "创建一个 npm 包的工程模板",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22",
    "bun": ">=1.0.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "biome check --write --no-errors-on-unmatched",
      "vitest related --run"
    ],
    "*.{json,md}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  }
}
